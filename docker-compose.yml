services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wsimpleapp
    volumes:
      - storage_data:/var/www/storage
    restart: unless-stopped
    ports:
      - "9000"
    networks:
      - nginx-net

networks:
  nginx-net:
    driver: bridge